import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

// Composition root for Explore structural tools. Task 00 registers nothing;
// later tasks wire tools, engine, and hooks here.
export default function exploreExtension(_pi: ExtensionAPI): void {}
