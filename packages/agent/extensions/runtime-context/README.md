# Runtime Context

Supplies Tau with the current local date and an initial root directory snapshot as hidden session context. The snapshot remains fixed until the extension runtime restarts, while the date updates when the local day changes.

After changing this extension, run `/reload` before testing the new behavior.
