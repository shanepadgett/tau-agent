# Run Summary

Run Summary adds a compact marker after the agent settles with no automatic continuation pending. It shows wall time, direct run cost, delegated subagent cost, and their combined total across the full continuation chain.

The marker is stored as a display-only session entry. It does not enter model context or trigger another agent turn.
