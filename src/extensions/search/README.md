# Tau Search

Tau Search provides `read`, `grep`, `find`, and `ls` for compact file exploration.

`workingMemory` can prune stale or forgotten search evidence and enables `forget`.

Search respects gitignore and common noise paths by default. Opt into ignored, hidden, or noise paths only with narrow paths, patterns, or globs.
