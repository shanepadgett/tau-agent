# Run Summary

Run Summary adds a compact marker after each agent run. It shows wall time, direct run cost, delegated subagent cost, and their combined total.

The marker is stored as a display-only session entry. It does not enter model context or trigger another agent turn.
