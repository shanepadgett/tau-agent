# `grep` (harness / Pi)

## Ownership

- **Pi/harness built-in.** Explore does not register `grep` ([stripped.md](../stripped.md)).

## Purpose

- Literal/regex content search (stock Pi / ripgrep behavior).

## Explore

- None in this rewrite. Per-file grouped density wrappers deferred. Structural shape search is [ast-search.md](../shape/ast-search.md).
