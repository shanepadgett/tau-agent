# `ls` (harness / Pi)

## Ownership

- **Pi/harness built-in.** Explore does not register `ls` ([stripped.md](../stripped.md)).
- This file records harness ownership so rewrite work does not reintroduce an Explore clone.

## Purpose

- List files/directories under roots (stock Pi behavior).

## Explore

- None in this rewrite. Density path-tree wrappers are deferred product work, not acceptance for the WASM structural spine.
