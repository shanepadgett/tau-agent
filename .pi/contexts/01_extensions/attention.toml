name = "Attention"
description = "Terminal notifications for ready, compacted, branched, and blocked agent states"

[feature]
description = "Attention prompts and notifications for blocked or idle agent states"
read = []
outline = ["packages/agent/extensions/attention/index.ts"]
references = ["packages/agent/extensions/attention/README.md"]
