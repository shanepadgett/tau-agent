name = "Tool Loader"
description = "Progressive session loading of specialist tool groups for prompt-cache-friendly tool schemas"

[extension-runtime]
description = "Register load_tools for web, image, and appshot groups; capture allowed specialist names at session_start; activate registered tools for a capability; restore loaded capabilities from branch load_tools toolResult details on start and session_tree when the session began with load_tools plus all specialists active"
read = [
  "packages/agent/extensions/tool-loader/index.ts",
  "packages/agent/extensions/tool-loader/README.md",
]
show = []
outline = []
references = [
  "packages/agent/extensions/tau-help/help.md",
]
