# Tool Preview

Local UI playground for approving custom tool row rendering.

Run `/tool-preview grep`, `/tool-preview find`, `/tool-preview ls`, `/tool-preview patch`, or `/tool-preview read` in Tau to show per-tool row states.
Run `/tool-preview` or `/tool-preview clear` to hide it.
