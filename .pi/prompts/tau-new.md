---
description: Scaffold Tau extensions, prompts, themes, or skills
argument-hint: "<extension|prompt|theme|skill> [name] [description]"
---
# /tau-new scaffold request

This request was generated by `/tau-new`; proceed with Tau customization scaffolding. Args: ${ARGUMENTS:-none}

If kind, name, description, or extension placement is missing, ask concise clarifying question(s) before editing.

Rules:

- Kind must be `extension`, `prompt`, `theme`, or `skill`.
- Extension placement must be `core` or `standalone`.
- Name must be kebab-case.
- Check target/name collisions before editing.
- Read relevant Pi docs first, following cross-references/examples as needed.
- Inspect current Tau package patterns before editing.

Pi docs to read (resolve via the default Pi docs/examples locations already in context):

- extension: Extensions, TUI, extension examples.
- prompt: Prompt Templates.
- theme: Themes.
- skill: Skills.

Targets:

- core extension: `src/extensions/core/src/<name>/`; wire from `src/extensions/core/index.ts`; update `src/extensions/core/README.md`.
- standalone extension: `src/extensions/<name>/index.ts` and `src/extensions/<name>/README.md`.
- prompt: `prompts/<name>.md`.
- theme: `themes/<name>.json`.
- skill: `skills/<name>/SKILL.md`.

Extension rules:

- Use native Pi TUI components when UI is needed.
- Put Tau custom extension events in `src/shared/events.ts`.
- Defer long-lived resources until `session_start` or the command/tool that needs them.
- Clean up session-scoped resources in `session_shutdown`.
- Store durable extension state in session entries or tool result details when appropriate.
- Add extra extension files only when they clearly improve readability.
